{
global int a = 500;
global int b = 600;
global int c = 500;


func void transfer(& int from, & int to, int amount) {
  if (from >= amount) {
		from -= amount;
		to += amount;
	} {};
};

func void lol () {
};

func void jee () {
};

print a;
print b;
transfer(a, b, 1);
print a;
print b;

}
