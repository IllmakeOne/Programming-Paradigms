{
global int jesse = 10000;
global int robert = 2000;
global int marieke = 99999;

func void helicopterMoney(& int acc, int amount) {
    acc += amount;
};

func void atm(& int acc, int amount) {
    if (acc >= amount) {
      acc -= amount;
    } {};
};

func void transfer(& int from, & int to, int amount) {
    if (from >= amount) {
        from -= amount;
        to += amount;
      } {};
};

func void test() {
  print jesse;
  print robert;
  print marieke;

  helicopterMoney(jesse, 100);
  helicopterMoney(robert, 100);
  helicopterMoney(marieke, 1);

  print jesse;
  print robert;
  print marieke;
};

test();

//comments examples
}
