{
global int jesse = 1000;
global int robert = 1000;
global int marieke = 5000;

print jesse;
print robert;
print marieke;

func void transfer(& int from, & int to, int amount) {
    if (from >= amount) {
        from -= amount;
        to += amount;
      } {};
};

func void helicopterMoney(& int to, int amount) {
    to += amount;
};

func void atm(& int from, int amount) {
  if (from >= amount) {
    from -= amount;
  } {};
};


fork helicopterMoney(jesse, 50);
fork helicopterMoney(robert, 500);
fork helicopterMoney(marieke, 50);

join;

print jesse;
print robert;
print marieke;

//comments examples
}
