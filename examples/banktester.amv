{
global int a = 500;
global int b = 600;
global int c = 500;
int cheese = 3;
func void transfer(& int from, & int to, int amount) {
  int random = 40;
  if (from >= amount) {
		from -= amount;
		to += amount;
	} {};
};

print a;
print b;
transfer(a, b, 1);
print a;
print b;

}
